RELAY_DOMAIN=relay.example.com
# Set your own unique password (at least 16 characters). Never commit .env.
# Single quotes keep characters such as $ literal in Docker Compose .env files.
RELAY_ADMIN_PASSWORD=''
