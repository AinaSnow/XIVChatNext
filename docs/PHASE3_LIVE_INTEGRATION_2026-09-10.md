# 第三阶段真实游戏私聊联调（2026-09-10）

本轮使用 Edelweiss Hare@Asura，好友 El Cid@Asura。游戏重启并登录后连接本机 `14777`，测试对象和正文由用户确认。无需额外角色或国服客户端。

## 已通过的实际检查

| 检查 | 观察结果 |
| --- | --- |
| 当前角色和好友 | 自动收到完整 42 人快照，Owner 与登录角色一致；El Cid 的母世界为 Asura（23），ContentId 非零，快照标记在线。 |
| 好友进入会话 | 从好友行打开 El Cid 会话，发送框明确标注固定目标 `El Cid @ Asura`。 |
| 固定目标发送 | 游戏当前频道为 `CWLS-4:IwantEatCha-han`，从会话发送约定正文后记录为 TellOutgoing，目标 El Cid；返回频道页仍为 CWLS-4。没有切换群聊发送目标。 |
| 游戏回显与回复 | 20:52:35 发送测试消息（运行序号 74），20:52:52 收到 `test-ok`（77）。界面显示“已提交游戏”，正式记录来自游戏回显，收到回复另有独立入站记录。 |
| 未读与已读 | 切到频道页后，20:54:43 收到 `unread-test`（83）；数据库未读数为 1。打开会话后未读归零，已读行从 373 推进至 379。 |
| 本地存储 | 三条测试消息的 Owner 和会话键全部匹配，均为 live 记录；发送一条、接收两条。原始聊天负载没有 ContentId，好友和会话身份快照有 ContentId，不依赖猜测合并历史。 |
| 头像 | 自动匹配并显示 El Cid 头像；本地映射 Lodestone ID 为 `27135722`，非手动绑定，缓存文件存在。 |
| 断开和重连 | 主动断开后仍能阅读三条私聊，发送不可用。重连后仍各一条，已读数保持 0；好友快照刷新成功。 |
| 去重与完整性 | 重连前后共 379 条消息，基线缺失 0、新增 0、重复消息标识 0、重复运行序号分组 0，SQLite `quick_check=ok`，schema v3。 |

首次连接提示旧运行历史存在缺口：此前游戏已重启，插件内存历史无法跨运行补齐。客户端保留旧记录并显示缺口，没有宣称补齐缺失区间。

## 联调发现与修复

断开后，输入区仍优先显示上次发送的“已提交游戏”结果。顶部离线状态和发送禁用正确，但底部提示容易误解。已调整优先级：当前会话不可发送时先显示离线/插件升级说明，连接可用时再显示发送结果。回归覆盖已有发送结果时失去连接的状态。

修复后的隔离 WinUI 回归 63/63 通过，包括上述提示优先级、私聊提交与失败恢复、未读及历史检查。正常入口客户端重新构建成功（0 错误、3 条既有 Windows App SDK 配置警告）。插件本轮无需修改或重新编译。

更新后再次启动并连接，最终共 380 条记录（基线 379 条均保留，另新增 1 条其他消息），重复为 0；三条测试私聊保持不变，已读状态保持 0。客户端停留在 El Cid 会话并保持连接。

## 范围限制

- 真实切角色未测：用户没有其他国际服角色，继续保留自动化双角色覆盖，不要求为此切国服。
- 本轮验证固定目标私聊、未读、持久化、头像和客户端重连。没有测试新的排本提醒、免打扰或通知点击定位；它们属于尚未实施的第四阶段。
- 没有自动操作游戏移动、排本、登录或插件重载，也没有向其他好友发送消息。
- `ContentId` 与 Lodestone ID 分开保存，不互相推算；好友在线状态只代表快照时间。
