# 分发与介绍页收尾

日期：2026-09-14。

## 已完成

- 小窗口左侧导航增加滚动，设置按钮固定在底部；1240 × 540 实际窗口检查通过。
- 桌面隔离回归 90 项断言通过，另外有完成标记；使用独立输出目录和测试配置文件，未发送真实游戏消息。
- 新增 `pack.ps1`，生成独立运行的 Windows x64 客户端与标准 Dalamud 插件 ZIP，带 SHA-256 校验文件。
- 按用户要求保留英、日、德、中、法运行库语言资源，删除 78 个其他原生语言目录；应用界面翻译仍为中英文。
- 插件清理旧打包输出，并直接使用 SDK 的最终 ZIP；主 DLL、清单位于根目录，无嵌套 ZIP。
- 实际分发启动检查发现并修复既有问题：项目强制启用框架 bootstrap，与分发包自带运行库的初始化冲突，出现 `CoreMessagingXP.dll / 0xc0000602`。移除强制设置，由 SDK 按部署方式选择，修复后最终包实际启动成功。
- 最终包恢复了保存的聊天小窗及本地历史；未出现重新配对或配置恢复对话框。当前无游戏进程，不能据此宣称在线重连已经验收。
- 产品介绍页包含功能、直连/中继、接入步骤、常见问题与 1Panel 部署指南；使用已有品牌 Logo 与虚构演示消息，无真实聊天数据或凭据。
- 两个静态页面及本地资源、锚点链接检查通过。介绍页在独立站点仓库保存并私有发布；未上传聊天项目源码或分发二进制。

## 交付

忽略目录 `artifacts/preview-20260914-ready/`：

- `XIVChatNext-Desktop-win-x64.zip`：73,358,950 字节，约 70.0 MiB。
- `XIVChatNext-Plugin.zip`：952,921 字节，约 0.91 MiB。
- `XIVChatNext-Website-1Panel.zip`：纯静态页面；解压后 `index.html` 直接位于网站根目录。
- `SHA256SUMS.txt`：三个 ZIP 的 SHA-256。

介绍页私有预览：<https://xivchat-next-hare.nemophilahare.chatgpt.site>。
站点源码单独保存在同级目录 `XIVChatNext-site`。

## 仍待正式发布前验证

游戏与插件重新启动后的完整在线重连、临时断网恢复、长期弱网负载，以及干净 Windows 环境的安装/升级/回滚。本轮结束前游戏已关闭，没有为验收启动或重启游戏，也未撤销现有授权。

当前产物仍是开发预览候选包，未更新公共插件源、公共镜像、GitHub Release 或正式版本号。
