# 第一阶段游戏联调记录

时间：2026-09-09 03:55–04:06（Asia/Shanghai）。被测提交：`66a4782`。

环境：Windows；Dalamud 15.0.3.3；当前分支的 Debug 插件与 WinUI 客户端；本机端口 14777；插件历史缓存开启，保留 100 条。用户已完成插件加载、角色登录和客户端配对连接。

## 实测结果

| 检查 | 证据 | 结果 |
| --- | --- | --- |
| 插件加载与连接 | 用户确认加载成功；14777 同时存在监听和本机 Established 连接；客户端显示已登录角色、世界与位置 | 通过 |
| 新协议与角色归属 | 初始 58 条消息均具有稳定消息 ID；序号、ServiceId、RunId 与数据库字段一致；58 条 Owner 均有 ContentId，Owner.Key 与存储分区一致 | 通过 |
| 客户端发送及实时接收 | 在客户端提交 `/echo XIVCHAT-LIVE-20260909-A 中文回显验证`；游戏与客户端均显示回显；数据库增加到 59 条，标记为 Echo、序号 59 | 通过 |
| 断线期间的历史补拉 | 客户端断开后，在游戏输入 `/echo XIVCHAT-LIVE-20260909-B offline-backfill`；断线时数据库仍为 59 条且没有 B；重连后 B 入库，序号 60，客户端显示一次 | 通过 |
| 重播去重与完整性 | 第一次重连后共 61 条（含一条自然产生的系统消息）；原始 58 条 ID 的哈希集合全部保留；消息 ID 和流内序号的重复分组均为 0 | 通过 |
| 持久化游标 | 初始同步游标为 0，实时接收未提前推进补拉检查点；第一次重连完成后游标和已存最大序号均为 61 | 通过 |
| 再次重连 | 第二次断开并使用已保存服务器重连后，数据库仍为 61 条，游标仍为 61；两个 Echo 标记各一条，客户端可见各一条 | 通过 |
| SQLite 检查 | 各次只读检查的 `PRAGMA quick_check` 均返回 `ok`；最终 61 条消息全部具有有效角色归属和匹配的流元数据 | 通过 |

数据库以 SQLite ReadOnly 模式在事务内读取。基线只保存消息 ID 的哈希；本记录不包含真实聊天内容、角色标识、服务器密钥或私人配置。测试使用的 `/echo` 消息仅自己可见，参见[官方命令说明](https://na.finalfantasyxiv.com/lodestone/playguide/db/text_command/87e36353238/)。联调结束后客户端保持连接。

## 范围与剩余验证

这次验证了第一阶段在单个真实登录角色上的发送、接收、落库、补历史和重连去重链路。没有发现需要追加代码修复的问题。

以下未在这次真实游戏会话中执行：切换角色及登录/登出过程、跨服后的身份稳定性、真实 Tell 对端解析、插件卸载/重载或更换端口、客户端进程重启后的恢复、大量消息与缓存溢出缺口。部分场景已有自动化回归覆盖，不能等同于真实游戏实测。后续阶段与整体游戏验收仍按 `WORKBENCH_PLAN.md` 推进。

## 04:18 角色掉线重连后的补充核验

用户自行操作了角色掉线重连，随后于 04:20–04:23 检查结果：

- 客户端恢复显示同一角色、世界与位置，输入框可用，14777 连接为 Established。
- 重连后的系统消息在 04:18:14–04:18:17 入库，序号为 65–75；消息流仍为同一个 source/ServiceId/RunId，所有记录仍属于同一个角色分区。
- 在客户端追加 `/echo XIVCHAT-LIVE-20260909-C after-character-relogin`，成功收到并显示一次，作为序号 76 的 Echo 写入数据库，确认重新登录后发送链路也可用。
- 最终 76 条消息，序号 1–76 连续，无重复序号或消息 ID，初始基线记录全部保留，SQLite quick_check 为 ok。
- 补拉游标仍为 61，符合实时消息不提前推进历史页检查点的设计；角色登录恢复不等同于桌面 TCP 连接重新握手。

本次确认的是用户操作后的恢复结果及后续收发。未现场观察掉线瞬间的输入禁用和状态清空，因此该瞬时行为仍待验证；也不能据此声称已验证切换到另一角色或强制断网异常路径。

## 04:31 私聊与公开头像查询的补充核验

用户明确指定了测试收件人，客户端发送了一条测试 Tell，随后收到该收件人的两条 Tell 回复。客户端可见三条私聊，数据库对应序号 79（TellOutgoing）、80 和 81（TellIncoming）。

- 三条记录的 TellPeer 均与指定收件人的姓名、母世界匹配，HomeWorldId 为 60；发送和接收共用同一个姓名与母世界组成的身份键，Owner 仍为当前登录角色。
- TellPeer.ContentId 在三个方向样本中均为 0。当前插件从 PlayerPayload 取姓名和世界，没有为对端填充 ContentId；这不代表 Lodestone ID，也不能以公开资料 ID 替代游戏 ContentId。
- 最终 81 条消息，序号 1–81 连续，重复消息 ID 和重复流内序号均为 0，原始基线完整，SQLite quick_check 为 ok。
- 使用指定姓名和母世界查询官方 Lodestone，搜索结果为 1 个；角色主页名称与母世界再次核对一致。成功从搜索结果的角色链接提取公开角色 ID，再从主页的 `frame__chara__face` 元素读取头像 URL，没有推算图片地址。
- 头像 URL 的 HEAD 请求返回 HTTP 200，Content-Type 为 image/jpeg，Content-Length 为 14158。没有下载头像文件或修改用户的头像映射表。

此项确认了真实 Tell 收发及对端姓名/世界解析，并验证在线查询该角色的 Lodestone ID 与头像地址可行。头像查询本次使用独立诊断流程；客户端的自动匹配、头像展示、缓存及手动修正仍属于第三阶段，尚未实现。公开资料 ID 不用于合并聊天历史或校验发送身份。
