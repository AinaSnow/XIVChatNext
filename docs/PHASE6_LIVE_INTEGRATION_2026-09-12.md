# 第六阶段截图实机联调

2026-09-12，用户确认新版插件截图正常后，在 `0f2e589` 上继续验证。游戏角色为 Edelweiss Hare @ Asura，客户端连接本机 14777 端口，游戏主视口为 1920 × 1080。未修改游戏显示设置、角色或装备；未发送聊天消息。

## 实测通过

- 用户原有截图为 20:44:23、1920 × 1080、约 383 KiB；重新点击标准档得到 20:45:58 的新画面，约 371 KiB。
- 切到清晰档得到 20:46:45 的新画面，579,303 字节（约 566 KiB）。游戏分辨率低于清晰档上限，因此维持 1920 × 1080，不放大。点击后约 893 ms 的界面观察已显示完成；这是观察上界，并非精确捕获耗时。
- 通过实际系统“另存为”窗口保存 JPEG，并从磁盘重新打开核对：游戏场景、角色、游戏 UI 与 Dalamud 顶栏均正常，未夹入遮挡游戏的桌面窗口。保存路径为本机 `artifacts/phase6/live/detailed-20260912-204645.jpg`，SHA-256 为 `2636248B29803F11D364FE2377E5D47F0EC33F830D2E92C11EE688EE817B2C6A`。
- 100% 按钮按原始像素展示，并出现横／纵滚动条；“适应窗口”恢复完整画面。
- 截图客户端置前、游戏在后台被遮挡时，连续七次清晰档截图成功，拍摄时间分别更新到 20:49:17、20:49:40、20:49:54、20:50:10、20:50:26、20:50:36、20:50:51。图片约 566–585 KiB；后六次均在约 1.64–1.67 秒的观察点前显示完成。
- 主客户端主动断开后，捕获按钮禁用，原图保留且明确标为“此前连接或角色的截图”，保存仍可用。重连本机服务器后旧图仍保持旧连接标记；再次捕获取得 21:06:01 的新图，约 800 KiB，来源恢复为当前角色。
- 主聊天窗口仍显示已连接，并收到 20:53 的新游戏消息；图片传输期间的协议 ping 另有下述量化检查。

## 连续捕获与队列响应

独立本机探针只在内存读取现有客户端密钥，并核对已经信任的插件公钥。未新增信任、未修改配置，订阅频道为空；只发送截图请求与 ping，不写入游戏聊天。两张请求起点至少相隔 2.2 秒，正常客户端保持连接。

30 张清晰档图片全部完整组装并通过摘要／尺寸校验，分辨率均为 1920 × 1080，拍摄时间持续更新。

| 指标 | 本机结果 |
| --- | --- |
| 请求到完整图片 | 263–280 ms，平均 270.7 ms |
| 收到第一片后发送 ping，到收到 pong | 0–1 ms（整数毫秒计时） |
| 测试跨度 | 约 64 秒 |
| 游戏私有内存，首／末张 | 9,511,837,696 / 9,679,761,408 字节 |
| 游戏私有内存，随后两次观察 | 9,569,894,400 / 9,432,174,592 字节 |
| 游戏句柄，首／末张及随后 | 2722 / 2737；随后回落到 2721 |

内存存在短时增长，但之后回落到首张以下；本轮未观察到持续累积或连接队列饥饿。这里测量的是整个游戏进程，包含其他插件，不是 GPU 显存归因，也不是长时间运行的无泄漏证明。协议耗时不包含 WinUI 解码和预览绘制。

探针源码和原始 JSON 在本机 `artifacts/phase6/probe/`；结果文件为 `bin/Debug/net9.0/repeated-results.json`。测试图及生成文件不纳入 Git。

## 验证边界

后台遮挡已实测；最小化／停止渲染尚未实测。当前游戏为无边框窗口，未提供可用的窗口最小化菜单；窗口管理工具启动的授权等待超时，未改用其他途径绕过。已请用户手动最小化，当前尚未收到完成回复，因此不将该项计为通过。

本轮没有登出、换角色、重载插件或变更游戏分辨率；这些不计为本轮现场通过。此前自动化的角色／登录轮次隔离、取消、超时、异常分片和旧插件兼容结果仍见 [实现与自动化记录](PHASE6_SCREENSHOTS_2026-09-12.md)。

本轮未改功能代码，因此未重复构建或运行离线测试；已恢复客户端连接，截图窗口保留重连后的新图。
