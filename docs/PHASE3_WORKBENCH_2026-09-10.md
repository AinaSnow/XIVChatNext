# 第三阶段：工作台实现与验收记录

状态：用户处理系统应用控制限制后，本地回归、WinUI 检查和正常桌面界面验证已通过。真实游戏定向私聊仍需后续联调。

## 实现范围

- WinUI 工作台侧栏提供会话、频道、好友和历史入口。频道沿用旧配置的名称、顺序、筛选和 Markdown 设置，补充稳定视图 ID。已有旧 ID 缺失或重复时生成独立 ID。
- 会话按连接来源、自身角色、对端姓名和母世界隔离。好友补充 ContentId 不改变既有会话归属。会话支持置顶、草稿、备注、最近消息、实时未读和已读位置。切换角色时保留原会话草稿并清空新角色的频道输入。
- 好友侧栏使用完整快照，显示缓存状态与身份不可用项；缺失身份不生成可发送目标。离线启动或选取历史角色可以恢复该角色的好友缓存。
- 私聊使用结构化的固定姓名和母世界目标，附带自身角色和登录轮次。发送时不依赖游戏当前频道；插件在游戏线程再次校验身份和世界名。旧插件没有相应能力时，会话提供离线草稿。
- 发送状态区分排队、提交游戏及拒绝；提交游戏不表示对方已收到。失败保留原文和后来输入的草稿，超时明确显示结果未确认。长消息失败后取消同一请求的剩余分段，并报告此前提交的分段数。
- 历史页支持关键词、人物、频道、日期、角色和连接来源、书签筛选，分页、高亮、消息备注和书签。同一角色及来源的前后文以只读聊天视图打开。历史关闭时，私聊频道继续订阅；新增消息不入库。
- Lodestone 头像按官方搜索中的唯一姓名和母世界匹配，分页、歧义和未知页面结构不自动绑定。支持手动 ID／官方链接、取消绑定、重新自动匹配、缓存及联网开关。网络任务合并、串行限流、失败退避、响应大小限制和取消检查均已接入。
- SQLite 升级到 v3，升级前创建原库备份。已有明确 TellPeer 的私聊记录回填会话键；旧记录不计实时未读。新增会话状态和头像禁用状态，保留原消息备注与书签。

## 已执行的验证

- 正常桌面入口和隔离 WinUI 检查入口编译成功；仅保留已有 Windows App SDK 单文件发布设置的 3 项告警。
- 插件隔离构建成功，7 项既有 Hook／可空性告警，0 个错误。
- 通用回归工程编译成功，0 个警告，0 个错误。
- 重新运行通用回归：34/34 通过。十万条历史写入 11.55 秒，三次筛选查询共 24 毫秒（本机测量）。数据库升级、会话隔离、已读、注释、取消和头像覆盖竞争均通过。
- WinUI：57 项检查通过，一万条消息仅创建 57 个消息控件。覆盖旧配置迁移、阅读位置、完整／无效好友入口、私聊固定目标、排队／提交状态、失败恢复、角色切换、书签和只读上下文，以及明暗主题的文本对比度。
- 插件正常开发输出的真实入口检查通过：构造函数到达配置服务，11 项服务注入完整，开发清单和调试符号齐备。隔离 `OutDir` 不经过项目原有清单打包路径，因此此检查使用正常 `bin/Debug` 输出。
- 正常桌面使用已有配置和数据库成功启动；v3 升级前自动备份已确认存在。离线会话恢复、中文历史搜索与高亮、本人及对端头像实际加载均已观察。关闭窗口后进程退出，重新启动正常。
- 给定 `22586842` 官方链接的 ID 提取检查通过。在线资料页本次未能通过浏览工具访问，因此未确认此角色头像可下载。
- 中英文资源键一致，新界面引用的资源完整，`git diff --check` 通过。

## 本轮修复

- 置顶、备注等状态更新可能将输入框重置为旧草稿。改为同步处理 `TextChanging`，并只在草稿变化时同步输入框。立即输入后修改置顶和备注的回归通过。
- 游戏原始浅色文字及白色时间戳在浅色工作台中难以阅读。文字按当前主题调整到至少 4.5:1 的目标对比度，主题改变后重新渲染，保留颜色区别。
- 补上离线角色标题；历史条目的辅助功能名称使用可读摘要。

## 已解除的运行阻碍

2026-09-10 09:27（Asia/Shanghai），系统返回 `0x800711C7`。只读检查 Code Integrity 日志确认 Smart App Control 拦截，事件 3077 指出本地构建未满足签名策略：

- `tests/Regression/bin/Debug/net9.0/XIVChatStorage.dll`
- `artifacts/workbench-desktop-smoke/XIVChat Desktop.exe`

此前没有更改或绕过安全策略。用户随后告知已处理 Smart App Control，再次运行后不再发生拦截。桌面与插件正常开发目录已更新；本轮没有运行游戏客户端。

## 后续联调范围

当前新增私聊发送检查使用本机模拟服务端。真实游戏的定向私聊、切角色和不同 DPI 下的界面仍需补测，不能由本地检查替代。实际向其他玩家发送测试私聊需要用户明确授权。

隔离桌面、WinUI 检查和插件分别输出到 `artifacts/workbench-desktop-build`、`artifacts/workbench-desktop-smoke`、`artifacts/workbench-plugin-build`。最终正常开发输出位于各项目的 `bin/Debug` 子目录，当前启动的桌面来自正常开发输出。构建与运行日志保存在 `artifacts/phase3-*.log`。

第四阶段事件和通知、第五阶段卡片和收藏、第六阶段多窗口和布局仍按原计划推进。
