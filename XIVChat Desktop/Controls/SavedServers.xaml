<UserControl x:Class="XIVChat_Desktop.Controls.SavedServers"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="using:XIVChat_Desktop"
             mc:Ignorable="d"
             x:Name="SavedServersControl"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <Grid Margin="8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <ListView x:Name="Servers"
                  Grid.Column="0"
                  ItemsSource="{x:Bind ItemsSource, Mode=OneWay}"
                  DoubleTapped="Item_DoubleTapped">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:SavedServer">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{x:Bind Name, Mode=OneWay}" />
                        <TextBlock Grid.Column="1" Text="{x:Bind Host, Mode=OneWay}" />
                        <TextBlock Grid.Column="2" Text="{x:Bind Port, Mode=OneWay}" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <StackPanel Grid.Column="1"
                   Orientation="Vertical"
                   Margin="8,0,0,0"
                   Visibility="{x:Bind ControlsVisibility, Mode=OneWay}">
            <Button HorizontalAlignment="Stretch"
                    Margin="0,0,0,4"
                    Click="AddServer_Click">
                Add
            </Button>
            <Button HorizontalAlignment="Stretch"
                    Margin="0,0,0,4"
                    Click="EditServer_Click">
                Edit
            </Button>
            <Button HorizontalAlignment="Stretch"
                    Click="DeleteServer_Click">
                Delete
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
