<Window x:Class="XIVChat_Desktop.ConnectDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="using:XIVChat_Desktop"
        xmlns:cc="using:XIVChat_Desktop.Controls"
        mc:Ignorable="d"
        Title="连接"
        d:DataContext="{d:DesignInstance local:ConnectDialog}">
    <Grid Margin="16" DataContext="{x:Bind}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <cc:SavedServers x:Name="Servers"
                         ItemsSource="{x:Bind App.Config.Servers, Mode=OneWay}"
                         ItemDoubleClick="Servers_ItemDoubleClick"
                         ControlsVisibility="Visible" />

        <StackPanel Grid.Row="1"
                   Grid.ColumnSpan="2"
                   Margin="0,16,0,0"
                   HorizontalAlignment="Right"
                   Orientation="Horizontal">
            <Button Margin="0,0,8,0"
                    Click="Cancel_Click">
                取消
            </Button>
            <Button Click="Connect_Clicked">
                连接
            </Button>
        </StackPanel>
    </Grid>
</Window>
