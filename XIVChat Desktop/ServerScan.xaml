<Window x:Class="XIVChat_Desktop.ServerScan"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="using:XIVChat_Desktop"
        xmlns:cc="using:XIVChat_Desktop.Controls"
        mc:Ignorable="d"
        Title="服务扫描"
        Closed="ServerScan_OnClosed"
        d:DataContext="{d:DesignInstance local:ServerScan}">
    <Grid Margin="8" DataContext="{x:Bind}" Loaded="ServerScan_OnLoaded">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <cc:SavedServers Grid.RowSpan="2"
                         x:Name="SavedServers"
                         ItemsSource="{x:Bind Servers, Mode=OneWay}"
                         ItemDoubleClick="SavedServers_OnItemDoubleClick"
                         ControlsVisibility="Collapsed" />

        <Button Grid.Column="1"
                Click="Add_Click"
                Margin="5,0,0,0"
                Content="添加" />
    </Grid>
</Window>
