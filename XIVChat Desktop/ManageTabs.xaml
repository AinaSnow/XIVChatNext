<Window x:Class="XIVChat_Desktop.ManageTabs"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="using:XIVChat_Desktop"
        mc:Ignorable="d"
        Title="管理标签"
        d:DataContext="{d:DesignInstance local:ManageTabs}">
    <Grid Margin="8" DataContext="{x:Bind}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <ListView x:Name="Tabs"
                  Grid.Column="0"
                  ItemsSource="{x:Bind App.Config.Tabs, Mode=OneWay}"
                  DoubleTapped="Tab_DoubleTapped">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:Tab">
                    <TextBlock Text="{x:Bind Name}" FontFamily="ms-appx:///Resources/fonts/ffxiv.ttf#XIV AXIS Std ATK" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <StackPanel Grid.Column="1"
                   Orientation="Vertical"
                   Margin="8,0,0,0">
            <Button HorizontalAlignment="Stretch"
                    Margin="0,0,0,4"
                    Click="AddTab_Click">
                添加
            </Button>
            <Button HorizontalAlignment="Stretch"
                    Margin="0,0,0,4"
                    Click="EditTab_Click">
                编辑
            </Button>
            <Button HorizontalAlignment="Stretch"
                    Click="DeleteTab_Click">
                删除
            </Button>
        </StackPanel>
    </Grid>
</Window>
